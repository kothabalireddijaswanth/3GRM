#include<stdio.h>
int main(void)
{
    int a , b, temp;
    printf("Enter a,b values");
    scanf("%d , %d",&a,&b);
    temp=a;
    a=b;
    b=temp;
    printf("After exchange a is %d , b is %d",a,b);
    grtch();
}
