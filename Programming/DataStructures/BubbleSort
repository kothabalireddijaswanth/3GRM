#include<stdio.h>
#include<conio.h>
void main()
{
	int a[100],i,j,n,s;
	printf("enter no.of elements");
	scanf("%d",&n);
	printf("enter the  interges into elements");
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	for(i=0;i<(n-1);i++)
	{
		for(i=0;i<(n-1);i++)
	{
		if(a[j]>a[j+1])
		{
			s=a[j];
			a[j]=a[j+1];
			a[j+1]=s;
		}
	}
	}
		printf("sorted list :");
		for(i=o;i<n;i++)
		printf("%d\n",a[i]);
		getch();
}
